包含以下特征的合约将通过抽取
１．合约中存在用于记录地址及其持有代币关系的账本型变量－mapping(address=>uint256)
２．合约中存在标识为payable的函数
３. 合约中存在一条路径，在该条路径下包含了对账本型变量的操作以及向外部转出以太币
４．合约中使用了.send/transfer/call.value等任意语句

路径条件：路径中要存在两条语句，即对账本变量的扣减操作和以太币的转出操作

先决条件：
寻找账本变量
１．在payable函数及其后续调用链中，存在对mapping(address=>uint256)型变量增加值的操作
２．在以太币转出函数及其前序、后续调用链中，存在对同一个mapping(address=>uint256)型变量减少值的操作
